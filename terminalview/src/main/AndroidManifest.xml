<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name=".TerminalInitProvider"
            android:authorities=".TerminalInitProvider"
            android:exported="false" />
    </application>
</manifest>
